{
  "name": "@electr0zed/test-results-dashboard",
  "private": true,
  "packageManager": "pnpm@11.7.0",
  "scripts": {
    "build": "turbo build --continue=always --filter='./packages/*'",
    "build:examples": "turbo build --continue=always --filter='./examples/*'",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.0",
    "turbo": "^2.9.18"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ELECTR0ZED/test-results-dashboard.git"
  }
}
