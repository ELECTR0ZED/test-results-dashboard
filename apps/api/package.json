{
  "name": "@electr0zed/test-results-dashboard-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@electr0zed/test-results-dashboard-core": "^0.1.0",
    "@electr0zed/test-results-dashboard-db": "^1.0.0",
    "hono": "^4.11.9"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "wrangler": "^4.64.0"
  }
}
